<UserControl x:Class="WPF_Flashapp.Views.UserControls.AppLogo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Flashapp.Views.UserControls" d:DataContext="{d:DesignInstance Type=local:AppLogo}"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="200"
             d:Background="#252737">
    <StackPanel VerticalAlignment="Center"
                Background="Transparent">
        <StackPanel.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="16,4,0,4"/>
            </Style>
        </StackPanel.Resources>
        <TextBlock Text="Flash Memory" 
                   Foreground="White" FontSize="25" FontWeight="Light"/>
        <TextBlock Text="Train Your Brain, Remember Your Past" Foreground="DarkGray" FontStyle="Italic" FontSize="10"/>
    </StackPanel>
</UserControl>
